importScripts("https://www.gstatic.com/firebasejs/10.7.1/firebase-app-compat.js");
importScripts("https://www.gstatic.com/firebasejs/10.7.1/firebase-messaging-compat.js");

// Note: To receive background notifications on the web, you would need to call 
// firebase.initializeApp({ ...your config... }) here. 
// However, just having this file present is enough to fix the 404 MIME type error
// and allow the app to generate a token for foreground testing.
